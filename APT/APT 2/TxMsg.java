public class TxMsg {
    public String getMessage(String original) {
      char[] newArray = original.toCharArray();
      String ret = "";
      ret += newArray[0];
      for(int i = 1; i < newArray.length; i ++){
        if(newArray[i] )
      }
    }
  }